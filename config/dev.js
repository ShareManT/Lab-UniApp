'use strict'

const config = {
  target: 'student',
  service: {
    app: {
      protocol: 'https',
      host: '',
    },
    passport: {
      protocol: 'https',
      host: ''
    },
    resource: {
      protocol: 'https',
      host: ''
    },
  },
};

module.exports = config;
